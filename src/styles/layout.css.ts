import { style } from "@vanilla-extract/css";

export const container = style({
  maxWidth: "1100px",
  margin: "0 auto",
  padding: "40px 20px",
});

export const section = style({
  marginTop: "60px",
});
